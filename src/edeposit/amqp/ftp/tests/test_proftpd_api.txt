*** Settings ***
Metadata    name  "Test ProFTPD API - create, rename, remove user and so on."
Library     inspector.Inspector
Library     OperatingSystem
Variables   inspector.py


*** Variables ***


*** Keywords ***
Create user
    [Arguments]  ${name}    ${pass}
    ${result} =  Call       ${ftp.api.add_user}         ${name}  ${pass}
    Log          ${result}
    [Return]     ${result}

Change password
    [Arguments]  ${name}    ${new_pass}
    ${result} =  Call       ${ftp.api.change_password}  ${name}  ${new_pass}
    Log          ${result}
    [Return]     ${result}

Remove user
    [Arguments]  ${name}
    ${result} =  Call       ${ftp.api.remove_user}      ${name}
    Log          ${result}
    [Return]     ${result}

# Check lock

# Check permissions


*** Test Cases ***
Check user manipulation
    Should fail       AssertionError  Create user  test,-  test

    ${random_name} =  Get random name
    ${random_pass} =  Get random name
    Create user       ${random_name}   ${random_name}
    # Remove user